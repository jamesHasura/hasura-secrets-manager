apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: basic-cluster
  region: us-east-1
managedNodeGroups:
  - name: secret-management
    instanceType: t2.small
    minSize: 2
    maxSize: 2
